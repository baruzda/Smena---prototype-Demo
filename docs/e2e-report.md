# E2E Report

Дата прогона: 17 июля 2026

Проверка выполнена в локальном браузере на `http://localhost:4173/` в мобильном viewport.

Запуск: `npm run test:e2e`. Набор находится в `tests/app.spec.mjs` и `tests/schedule-utils.spec.mjs`, запускает Vite через `playwright.config.mjs`.

| Сценарий | Результат |
| --- | --- |
| Стартовый экран, дефолтный фильтр «подходит мне» и карточки на 14 дней | PASS |
| Первый вход: подсказка настроек, закрытие по тапу и переход по выделенной кнопке | PASS |
| Переход по датам таймлайна и плавная прокрутка к выбранному дню | PASS |
| Скрытие нижней навигации при движении вниз, фиксированный таймлайн и кнопка возврата наверх | PASS |
| Сортировка: «сначала ближайшие», «рекомендуемые», «выше заработок» | PASS |
| Включение и отключение фильтра «подходит мне» | PASS |
| Поиск адреса, выбор радиуса, согласованное сохранение точки и состояния после перезагрузки | PASS |
| Карта Leaflet/OSM: загрузка тайлов, круг радиуса и применение точки поиска | PASS |
| Каждый радиус от `1` до `50 км` полностью помещается над нижней шторкой карты | PASS |
| Неудачный поиск адреса: блокировка применения и возврат к подтверждённой точке | PASS |
| Настройки расписания: открытие календаря через селектор, блокировка занятых дней, выбор свободного дня | PASS |
| Мультивыбор длительности задания и сохранение нескольких чипов | PASS |
| Подтверждение календаря, ручной ввод времени, сброс пресета времени | PASS |
| Применение доступности и пустое состояние «Нет подходящих смен» | PASS |
| Карточка → детали → «записаться» → успешная запись → «Мои задания» | PASS |
| Рабочие табы «мои задания» и «задания на подписание» | PASS |
| Кнопка «на карте» открывает сценарий карты | PASS |
| Сохранение записи, фильтров и настроек в `localStorage` после перезагрузки | PASS |
| Активные дата и таб не получают лишнюю фокус-обводку после тапа | PASS |
| Блокировка повторной и конфликтующей записи, включая пересечение через полночь | PASS |
| Производственная сборка | PASS (`npm run build`) |

## Ручной регрессионный чек-лист

- После изменения CSS или структуры карточек повторить скриншот на старте и после прокрутки минимум на две карточки.
- После изменения стартовой подсказки проверить первый вход, закрытие тапом вне подсказки и повторный вход после перезагрузки.
- После изменения логики таймлайна проверить, что выбранная дата не вызывает скачок горизонтальной ленты.
- После изменения карты проверить сценарии: клик по карте, ручной поиск адреса, изменение радиуса и применение результата.
- После изменения фильтра доступности проверить режим без ручного выбора и режим с выбранными свободными датами.
